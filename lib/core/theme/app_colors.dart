import 'package:flutter/material.dart';

/// App color palette
class AppColors {
  const AppColors._();

  // Brand colors
  static const Color primary = Color(0xFF5B9364);
  static const Color accent = Color(0xFF6358DE);
  static const Color danger = Color(0xFFED343D);
  static const Color dark = Color(0xFF141414);

  // Text colors
  static const Color text = Color(0xFF1e2939);
}
